# Tower RNG 필수 문서·구현 가이드

이 문서는 Tower RNG의 기획, 문서 작성, 기술 설계와 실제 구현에 참여하는 사람 및 AI가 가장 먼저 읽어야 하는 필수 가이드입니다.

모든 작업은 현재 대화나 한 문서만 보고 진행하지 않습니다. 관련된 상위·하위 문서를 확인하고, 결정된 내용과 미확정 내용을 구분한 뒤 작업합니다.

---

## 1. 최우선 원칙

1. 기능을 만들거나 수정하기 전에 이 문서를 먼저 읽습니다.
2. 사용자가 확정하지 않은 기능, 수치, 예외 규칙을 임의로 추가하지 않습니다.
3. 확정안, 임시 밸런스 값, 제안, 미확정 사항을 명확히 구분합니다.
4. 하나의 문서에 서로 다른 구현 시스템을 불필요하게 합치지 않습니다.
5. 기획 의도를 코드에 바로 옮기지 않고 아래 문서 계층을 순서대로 거칩니다.
6. 문서끼리 또는 문서와 코드가 충돌하면 임의로 한쪽을 선택하지 않습니다. 충돌 내용을 정리해 사용자에게 질문하고 승인받은 뒤 진행합니다.
7. 둘 이상의 합리적인 선택지 중 하나를 확정해야 하거나, 새로운 공개 규칙·수치·제약·사용자 흐름이 필요한 경우 반드시 사용자 승인을 받습니다.
8. 구현 편의를 이유로 게임의 핵심 철학을 약화하지 않습니다.
9. 실패, 손실, 강제 접속, 빌드 함정과 불필요한 스트레스를 새로 만들지 않습니다.
10. 좋은 새 타워를 얻었을 때 기존 투자 때문에 사용하기 어려워지는 구조를 피합니다.
11. 구현 후에는 실제 코드 계약과 문서가 일치하는지 확인합니다.

---

## 2. 문서 계층

기능은 다음 네 계층으로 관리합니다.

```text
게임 기획
    ↓
시스템 명세
    ↓
기술 설계
    ↓
구현 명세
    ↓
실제 코드
```

각 계층은 같은 내용을 복사하는 문서가 아닙니다. 아래 단계로 갈수록 더 구체적인 질문에 답합니다.

### 2.1 게임 기획 — 무엇을 왜 만드는가

경로:

```text
docs/design/<SYSTEM>.md
```

다루는 내용:

- 플레이어가 경험할 기능
- 기능의 목적
- 게임 규칙의 큰 방향
- 다른 시스템과의 관계
- 게임 철학상 허용하거나 금지하는 구조
- 확정된 기본 수치와 임시 밸런스 값
- 아직 결정되지 않은 문제

포함하지 않는 내용:

- ModuleScript 이름
- 함수명과 매개변수
- RemoteEvent 이름
- DataStore 키
- 구체적인 클래스와 폴더 구조

기획 단계에서도 구현 가능성을 생각해야 하지만, 구현 수단을 확정하지는 않습니다.

### 2.2 시스템 명세 — 정확히 어떻게 작동하는가

경로:

```text
docs/spec/<SYSTEM>.md
```

다루는 내용:

- 입력과 출력
- 정확한 동작 순서
- 판정 우선순위
- 수식과 수치 규칙
- 동률 처리
- 실패와 취소 처리
- 경계 조건
- 저장 또는 복구되어야 하는 상태
- 플레이어에게 표시되는 결과
- 테스트 가능한 수용 조건

이 문서는 구현 방식과 무관한 시스템의 행동 계약입니다.

예시:

```text
선두 타깃
1. 살아 있고 공격 가능한 몬스터만 후보로 사용한다.
2. PathProgress가 높은 순서로 정렬한다.
3. 동률이면 SpawnSequence가 빠른 대상을 우선한다.
4. 예약 피해로 사망 예정인 대상은 건너뛸 수 있다.
```

### 2.3 기술 설계 — 어떤 구조로 구현하는가

경로:

```text
docs/technical/<SYSTEM>.md
```

다루는 내용:

- 서버와 클라이언트의 책임
- 데이터 소유권과 데이터 흐름
- 런타임 상태 구조
- 시스템과 모듈의 경계
- 네트워크 통신 방향
- 저장과 복구 전략
- 갱신 주기와 캐시
- 성능 전략
- 보안과 악용 방지
- 기존 시스템과의 의존 관계
- 장애 시 안전한 동작

함수 전체 목록보다 구조와 책임 분리를 우선합니다.

### 2.4 구현 명세 — 실제 코드 계약은 무엇인가

경로:

```text
docs/implementation/<SYSTEM>.md
```

다루는 내용:

- 실제 파일 경로
- ModuleScript 책임
- 공개 타입
- 공개 함수와 반환값
- RemoteEvent와 RemoteFunction 계약
- Attribute와 CollectionService 태그
- 설정 데이터 형식
- 저장 스키마
- 오류 처리 방식
- 테스트 대상
- 구현 상태

모든 내부 보조 함수까지 기록하지 않습니다. 다른 모듈이 의존하는 공개 계약을 중심으로 작성합니다.

---

## 3. 문서 분리 기준

문서는 대화 주제가 아니라 **구현 책임 단위**로 나눕니다.

다음 조건 중 하나라도 해당하면 별도 문서를 우선합니다.

- 별도의 런타임 상태를 가짐
- 별도의 저장 데이터가 필요함
- 독립된 UI 또는 사용자 흐름을 가짐
- 별도의 서비스나 모듈로 구현될 가능성이 높음
- 다른 기능 없이도 규칙을 설명할 수 있음
- 향후 독립적으로 변경될 가능성이 높음

예시:

```text
CURRENCY.md
POTIONS.md
```

재화로 포션을 구매하더라도 재화 시스템과 포션 효과 시스템은 구현 책임이 다르므로 분리합니다.

반대로 하나의 작은 규칙을 의미 없이 여러 문서로 쪼개지는 않습니다. 한 시스템을 이해하기 위해 지나치게 많은 문서를 열어야 한다면 분리가 과도한 것입니다.

---

## 4. 기능 문서 작성 순서

새 기능은 다음 순서로 작성합니다.

### 단계 1: 시스템 경계 결정

먼저 다음 질문에 답합니다.

- 이 기능의 핵심 책임은 무엇인가?
- 별도의 저장 상태가 있는가?
- 별도의 UI가 있는가?
- 다른 시스템과 독립적으로 변경될 수 있는가?
- 한 문서에 합치면 구현 모듈도 불필요하게 결합되는가?

### 단계 2: 기획 문서 작성

플레이어 경험과 확정된 규칙을 작성합니다.

반드시 구분할 항목:

- 확정된 내용
- 임시 밸런스 값
- 미확정 사항
- 사용하지 않기로 한 구조

### 단계 3: 시스템 명세 작성

모호한 표현을 판정 가능한 규칙으로 바꿉니다.

나쁜 예:

```text
적절한 적을 공격한다.
```

좋은 예:

```text
우선순위 후보 5개를 선정하고 각 후보 주변의 실제 월드 좌표 밀집도를 비교한다.
```

### 단계 4: 기술 설계 작성

서버 권한, 저장, 네트워크, 성능과 시스템 경계를 결정합니다.

### 단계 5: 구현 명세 작성

실제 파일, 타입, 공개 함수와 데이터 계약을 확정합니다.

### 단계 6: 코드 구현

구현 명세에 없는 새 공개 계약을 임의로 만들지 않습니다. 필요한 경우 먼저 문서를 갱신합니다.

### 단계 7: 검증 및 문서 동기화

- 테스트 결과가 시스템 명세와 일치하는지 확인
- 함수와 데이터 계약이 구현 명세와 일치하는지 확인
- 변경된 공개 규칙을 상위 문서에도 반영
- 미구현 항목을 완료된 것처럼 표시하지 않음

---

## 5. 모든 기능에서 구현 단계까지 반드시 고려할 항목

기획 문서에서 아래 항목을 모두 자세히 결정할 필요는 없습니다. 그러나 이후 단계에서 문제가 되지 않도록 영향 여부는 반드시 생각합니다.

### 5.1 상태와 소유권

- 어떤 상태가 존재하는가?
- 서버와 클라이언트 중 누가 진실의 원본을 가지는가?
- 플레이어별 상태인가, 서버 공용 상태인가?
- 세션 종료 후 저장되어야 하는가?
- 동시에 여러 요청이 오면 어떤 순서로 처리하는가?

게임 결과, 재화, 타워 획득과 전투 판정은 기본적으로 서버가 검증합니다.

### 5.2 저장과 복구

- 저장할 데이터는 무엇인가?
- 저장 중 서버가 종료되면 어떻게 되는가?
- 중복 지급이나 중복 차감을 어떻게 막는가?
- 스키마가 변경될 때 기존 데이터를 어떻게 이전하는가?
- 오프라인 시간에 진행되거나 정지하는 값은 무엇인가?

### 5.3 네트워크

- 클라이언트가 서버에 무엇을 요청하는가?
- 서버가 어떤 값을 검증하는가?
- 다른 플레이어에게 어떤 정보가 복제되는가?
- 개인 전투 정보가 다른 플레이어에게 불필요하게 전송되지 않는가?
- 반복 호출과 잘못된 인자를 어떻게 제한하는가?

### 5.4 성능

- 매 프레임 계산이 필요한가?
- 일정 주기 갱신으로 충분한가?
- 몬스터와 타워 수가 증가해도 계산량이 감당 가능한가?
- 전체 탐색을 공간 인덱스나 캐시로 줄일 수 있는가?
- 다른 플레이어의 보이지 않는 전투 연출을 생성하고 있지 않은가?

최적화는 실제 병목을 기준으로 하지만, 명백한 전체 반복과 불필요한 복제는 초기 설계에서 피합니다.

### 5.5 보안과 악용 방지

- 클라이언트가 재화, 피해, 굴림 결과를 직접 결정하지 않는가?
- 편성 교체로 공격 재사용 대기시간을 초기화할 수 없는가?
- 요청을 빠르게 반복해 중복 구매나 중복 보상을 받을 수 없는가?
- 포션 시간과 저장 데이터를 조작할 수 없는가?
- 잘못된 ID나 소유하지 않은 타워를 사용할 수 없는가?

### 5.6 실패와 예외

- 대상이 공격 직전에 사라지면 어떻게 되는가?
- 서버 이동이나 연결 종료 중이면 어떻게 되는가?
- 데이터가 일부 누락되었을 때 안전한 기본값은 무엇인가?
- 잘못된 맵 경로나 설정 데이터가 있으면 어떻게 감지하는가?
- 실패 시 플레이어 자산을 잃지 않도록 처리되는가?

### 5.7 UI와 피드백

- 플레이어가 현재 상태를 이해할 수 있는가?
- 행동할 수 없는 이유를 구체적으로 보여주는가?
- 임시 효과의 남은 시간과 적용 범위를 확인할 수 있는가?
- 다른 플레이어의 표시가 내 전투를 방해하지 않는가?
- 희귀 결과와 중요한 변화가 전투를 막지 않는가?

### 5.8 테스트 가능성

- 규칙을 자동 테스트로 검증할 수 있는가?
- 수치와 시간을 테스트용 설정으로 바꿀 수 있는가?
- 랜덤 결과에 고정 시드나 테스트 입력을 사용할 수 있는가?
- 서버와 클라이언트 경계를 따로 검증할 수 있는가?
- 저장 실패와 중복 요청을 재현할 수 있는가?

---

## 6. 요구사항 ID

중요한 규칙에는 시스템별 짧은 요구사항 ID를 사용합니다.

예시:

```text
CUR-001  일반 성장 재화는 영구 성장에 사용한다.
CUR-002  연금 정수는 포션 구매에만 사용한다.
POT-001  같은 포션은 효과가 중첩되지 않고 시간이 연장된다.
TGT-001  선두 판정은 PathProgress를 기준으로 한다.
```

같은 ID를 하위 문서에서 연결합니다.

```text
기획: CUR-002의 목적과 플레이어 경험
명세: CUR-002의 정확한 획득·소비 규칙
기술: CUR-002를 소유하고 저장하는 시스템
구현: CUR-002를 처리하는 공개 함수와 데이터 필드
```

모든 문장에 ID를 붙이지 않습니다. 구현과 테스트에서 추적할 가치가 있는 핵심 규칙에만 사용합니다.

---

## 7. 문서 상단 필수 메타데이터

각 시스템 문서는 가능한 한 다음 형식을 사용합니다.

```markdown
# 시스템 이름

- 계층: 게임 기획 | 시스템 명세 | 기술 설계 | 구현 명세
- 상태: Draft | Confirmed | Implementing | Implemented | Deprecated
- 상위 문서: `경로`
- 하위 문서: `경로`
- 관련 문서: `경로`
- 마지막 정리: YYYY-MM-DD
```

그 아래에 짧은 `요약`을 둡니다. AI와 개발자가 전체 문서를 읽기 전에 범위와 핵심 결정을 파악할 수 있어야 합니다.

---

## 8. 토큰 친화적인 작성 규칙

1. 한 문서에는 한 시스템을 중심으로 작성합니다.
2. 같은 설명을 여러 계층에 복사하지 않고 링크와 요구사항 ID로 연결합니다.
3. 문서 첫 부분에 핵심 요약을 둡니다.
4. 토론 과정 전체를 본문에 남기지 않고 최종 결정과 짧은 이유만 기록합니다.
5. 미확정 내용은 문서 마지막의 `Open Questions`에 모읍니다.
6. 임시 수치는 `Provisional Balance Values`로 따로 표시합니다.
7. 폐기한 대안은 장기적인 구조 결정일 때만 `docs/decisions/`에 기록합니다.
8. 구현 명세에는 공개 인터페이스만 기록하고 내부 보조 함수는 코드에 둡니다.
9. 거대한 통합 문서를 만들지 않습니다.
10. 관련 없는 문서를 모두 읽게 하지 않고 현재 시스템의 수직 문서만 읽게 합니다.

---

## 9. 중요한 결정 기록

장기적으로 여러 시스템에 영향을 주는 결정은 ADR로 남길 수 있습니다.

경로:

```text
docs/decisions/ADR-###-SHORT-NAME.md
```

ADR이 필요한 예:

- 추종형 타워 전투 채택
- 공격 사거리 제거
- PathProgress와 WorldPosition 이중 인덱스 사용
- 베이스 체력 자동 회복 구조

ADR에 포함할 내용:

- 결정
- 이유
- 채택하지 않은 대안
- 영향을 받는 시스템

작은 밸런스 변경이나 포션 가격 변경에는 ADR을 만들지 않습니다.

---

## 10. 기존 기능 수정 절차

기존 기능을 수정할 때는 다음 순서를 따릅니다.

1. 해당 시스템의 기획 문서를 읽습니다.
2. 존재한다면 명세, 기술, 구현 문서를 함께 읽습니다.
3. 실제 코드가 문서와 일치하는지 확인합니다.
4. 변경이 어느 계층에서 시작되는지 판단합니다.
5. 상위 규칙이 변하면 상위 문서부터 아래로 갱신합니다.
6. 내부 구현만 변하고 공개 동작이 같으면 기술·구현 문서만 갱신합니다.
7. 코드를 변경합니다.
8. 테스트와 문서 동기화를 완료합니다.

한 파일만 고치고 관련 계약 문서를 방치하지 않습니다.

---

## 11. 충돌·미확정·승인 처리

다음 행동을 금지합니다.

- 미확정 수치를 확정값처럼 구현
- 예시를 필수 규칙으로 해석
- 제안된 기능을 사용자 승인 없이 추가
- 편의를 위해 새로운 재화, 제한, 쿨다운 또는 손실 추가
- 기획에 없는 자동화나 추천 기능 추가
- 기존 시스템과 충돌하는 규칙을 조용히 선택
- 둘 이상의 설계안 중 하나를 임의로 확정
- 사용자 경험이나 공개 계약을 바꾸는 결정을 내부 구현 판단으로 처리

다음 상황에서는 반드시 작업을 멈추고 사용자에게 질문합니다.

1. 문서끼리 내용이 충돌함
2. 문서와 실제 코드가 서로 다름
3. 구현을 위해 새로운 게임 규칙이나 수치가 필요함
4. 두 개 이상의 합리적인 구현이 사용자 경험에 서로 다른 영향을 줌
5. 기존 확정안을 폐기하거나 범위를 넓혀야 함
6. 기능을 합치거나 분리하는 선택이 저장·UI·공개 API에 영향을 줌

질문할 때는 다음 형식을 우선합니다.

- 충돌하거나 미확정인 항목
- 현재 확인된 기존 결정
- 가능한 선택지와 각각의 영향
- 추천안이 있다면 추천 이유

승인 전에는 기존 확정 내용을 정리하거나 영향 없는 내부 작업만 진행할 수 있습니다. 사용자 승인이 필요한 부분은 `Open Questions`로 남기며 확정된 것처럼 작성하거나 구현하지 않습니다.

안전한 임시값조차 사용자 경험이나 경제 밸런스에 영향을 준다면 임의로 사용하지 않습니다. 구현을 계속하기 위한 순수한 테스트용 내부값만 `Provisional`로 표시해 사용할 수 있으며, 실제 출시 설정으로 간주하지 않습니다.

---

## 12. 구현 완료 기준

기능은 코드가 실행된다는 이유만으로 완료되지 않습니다.

완료 조건:

- 기획 의도와 일치함
- 시스템 명세의 수용 조건을 만족함
- 서버와 클라이언트 책임이 명확함
- 재화와 주요 상태가 서버에서 검증됨
- 저장과 재접속 동작이 정의됨
- 중복 요청과 악용 가능성이 검토됨
- 일반적인 실패 상황에서 자산 손실이 없음
- 성능상 명백한 전체 반복과 불필요한 복제가 없음
- UI가 성공과 실패 이유를 전달함
- 공개 함수와 데이터 계약이 구현 명세와 일치함
- 테스트 또는 재현 절차가 존재함
- 관련 문서가 현재 코드 상태로 갱신됨

---

## 13. 현재 권장 디렉터리 구조

```text
README.md
AGENTS.md

docs/
├─ INDEX.md
├─ design/
│  └─ 플레이어 경험과 게임 규칙
├─ spec/
│  └─ 구현 방식과 무관한 정확한 동작
├─ technical/
│  └─ 아키텍처, 데이터 흐름, 성능과 보안
├─ implementation/
│  └─ 파일, 타입, 공개 함수와 데이터 계약
├─ decisions/
│  └─ 장기적인 주요 결정
└─ reference/
   └─ 공통 용어, enum과 데이터 표
```

기존 문서는 한 번에 모두 재배치하지 않습니다. 관련 시스템을 다시 다룰 때 이 구조에 맞춰 점진적으로 분리합니다.

---

## 14. 작업 시작 전 체크리스트

```text
[ ] AGENTS.md를 읽었는가?
[ ] 작업할 시스템의 경계를 정했는가?
[ ] 관련 design 문서를 읽었는가?
[ ] 존재하는 spec, technical, implementation 문서를 확인했는가?
[ ] 확정안과 미확정 사항을 구분했는가?
[ ] 문서 또는 코드 사이에 충돌이 없는가?
[ ] 사용자 승인이 필요한 선택이 없는가?
[ ] 다른 시스템 문서와 합치지 않아도 되는가?
[ ] 서버 소유권과 저장 영향을 고려했는가?
[ ] 네트워크와 악용 가능성을 고려했는가?
[ ] 성능과 실패 상황을 고려했는가?
[ ] 테스트 가능한 규칙으로 표현했는가?
```

## 15. 작업 종료 전 체크리스트

```text
[ ] 사용자 승인 없이 기능을 추가하거나 선택지를 확정하지 않았는가?
[ ] 발견한 충돌과 미확정 사항을 숨기지 않았는가?
[ ] 공개 동작이 기획·명세와 일치하는가?
[ ] 공개 함수와 데이터가 구현 명세와 일치하는가?
[ ] 임시값을 확정값처럼 남기지 않았는가?
[ ] 관련 문서 링크가 올바른가?
[ ] 미구현 항목을 완료로 표시하지 않았는가?
[ ] 저장, 재접속과 중복 요청을 확인했는가?
[ ] 실패 시 플레이어 자산이 안전한가?
[ ] 테스트 또는 검증 절차를 남겼는가?
```