# Tower RNG 필수 문서·구현 가이드

이 문서는 Tower RNG의 기획, 계산, 콘텐츠 작성, 기술 설계와 구현에 참여하는 사람 및 AI가 가장 먼저 읽어야 하는 필수 가이드입니다.

현재 대화나 단일 파일만 보고 작업하지 않습니다. 관련 상위·하위 문서와 `docs/PROJECT_STATUS.md`를 확인하고 확정안·임시 밸런스 값·제안·미확정 사항을 구분합니다.

---

## 1. 최우선 원칙

1. 기능이나 수치를 만들기 전에 이 문서와 관련 권위 문서를 확인합니다.
2. 사용자가 확정하지 않은 기능·수치·제약·손실을 임의로 추가하지 않습니다.
3. 확정안, 임시 밸런스 값, 제안과 미확정 사항을 명확히 구분합니다.
4. 문서끼리 또는 문서와 코드가 충돌하면 임의로 한쪽을 선택하지 않습니다.
5. 둘 이상의 합리적 선택지가 플레이 경험을 다르게 만들면 사용자 승인을 받습니다.
6. 구현 편의를 이유로 핵심 철학을 약화하지 않습니다.
7. 실패·강제 접속·빌드 함정·영구 손실과 불필요한 스트레스를 새로 만들지 않습니다.
8. 좋은 새 타워를 얻었을 때 기존 투자 때문에 사용하기 어려운 구조를 피합니다.
9. 기획 의도를 코드에 바로 옮기지 않고 문서 계층을 순서대로 거칩니다.
10. 구현 후 실제 코드 계약과 문서가 일치하는지 검증합니다.
11. 하위 계층이 변경되면 영향을 받는 상위 문서를 같은 작업에서 갱신합니다.
12. 의미 있는 작업이 끝나면 `docs/PROJECT_STATUS.md`의 체크리스트와 다음 작업을 함께 갱신합니다.
13. 계산과 카탈로그를 같은 문서에서 독립 원본으로 관리하지 않습니다.
14. 일반 UI 클릭과 일반 공격에는 화면 흔들림을 사용하지 않습니다. 제한된 보스·환생·극희귀 사건만 `SETTINGS.md` 범위에서 허용합니다.
15. 모든 의미 있는 UI 입력과 전투 결과는 즉각적이고 분명한 움직임·시각·음향 피드백을 가져야 합니다.
16. 자동 합체와 플레이어 간 게임 자산 거래는 현재 확정 범위에 없습니다.
17. 최초 튜토리얼은 계정당 한 번이며 재접속·환생·중복 요청으로 보상이 재지급되지 않아야 합니다.

---

## 2. 프로젝트 제어 문서

| 문서 | 책임 |
|---|---|
| `README.md` | 게임 루프와 V1 범위 |
| `docs/INDEX.md` | 전체 문서 탐색 |
| `docs/PROJECT_STATUS.md` | 진행률·마스터 체크리스트·현재 다음 작업 |
| `docs/catalog/INDEX.md` | 콘텐츠 카탈로그 책임과 채택 규칙 |
| `docs/balance/INDEX.md` | 계산·검증 책임과 남은 계산 |

작업 순서는 `docs/PROJECT_STATUS.md`의 현재 작업 ID를 따릅니다. 새로운 우선순위를 임의로 끼워 넣지 않습니다.

---

## 3. 권위 문서 지도

| 영역 | 권위 문서 | 하위 구현이 바꿀 수 없는 핵심 |
|---|---|---|
| 프로젝트 전체 | `README.md`, `docs/INDEX.md`, `docs/PROJECT_STATUS.md` | 최신 루프·범위·작업 순서 |
| 확률·행운 | `docs/design/RNG_PROBABILITY.md` | 공식 `1/N`, 임의 정밀도, 단일 결과, 현재 확률 비공개 |
| 통합 수치 | `docs/design/BALANCE_MODEL.md` | PowerBudget, StageScale, SpawnCost, 보상 인플레이션 |
| 웨이브 템포 | `docs/design/WAVE_PACING.md` | Entry·Farm 목표, 즉시 종료, 자동 적응 금지 |
| 스테이지 검증 | `docs/design/STAGE_VALIDATION.md` | 완전·경량 검증과 승격 조건 |
| 첫 환생 경제 | `docs/design/ECONOMY_PACING.md`, 현재 `docs/reference/FIRST_REBIRTH_ECONOMY_BENCHMARK.md` | 초기 가격·문·StageCoinUnit·첫 환생 |
| 저장·복구 | `docs/technical/STATE_LIFECYCLE.md` | 서버 권위, 단일 프로필, 세션 잠금, 원자성 |
| UI·전투 감각 | `docs/design/PRESENTATION_FEEL.md` | 피드백과 완료 기준 |
| 설정·접근성 | `docs/design/SETTINGS.md` | 카메라·파티클·음향·성능 설정 |
| 실제 콘텐츠 | `docs/catalog/INDEX.md`와 등록된 카탈로그 | ID·이름·채택 수치·자산 |
| 계산·검증 | `docs/balance/INDEX.md`와 등록된 계산 문서 | 가정·수식·시뮬레이션·수용 조건 |

기존 `docs/reference`는 링크 호환 영역입니다. 신규 문서는 추가하지 않습니다.

---

## 4. 카탈로그와 계산 분리

### 콘텐츠 카탈로그

경로:

```text
docs/catalog/<CATALOG>.md
```

소유하는 것:

- 영구 ID
- 최종 또는 잠정 채택 이름
- 채택된 수치
- 역할·태그·행동 계약
- 모델·애니메이션·VFX·SFX 요구사항
- Proposed·Confirmed·Implemented 상태

소유하지 않는 것:

- 역산 과정
- 몬테카를로 표
- 가정별 비교
- 민감도 분석
- 전체 검증 로그

카탈로그 수치는 가능한 경우 `BalanceSource`와 `BalanceStatus`를 기록합니다.

### 계산·검증

경로:

```text
docs/balance/<BENCHMARK>.md
tools/balance/<SCRIPT>.py
```

소유하는 것:

- 가정과 수식
- 입력 프로필
- 시뮬레이션 결과
- 수용·실패 조건
- 민감도
- 카탈로그 권장값

소유하지 않는 것:

- 최종 콘텐츠 이름과 자산 확정
- 구현 데이터의 독립 원본

채택 흐름:

```text
계산 완료
→ 수용 조건 통과
→ 카탈로그 채택
→ 구현 데이터 반영
→ 런타임 검증
```

계산용 임시 이름은 카탈로그 완료로 세지 않습니다.

---

## 5. 문서 계층

### 게임 기획

경로: `docs/design/<SYSTEM>.md`

- 플레이어 경험과 목적
- 큰 규칙과 시스템 관계
- 허용·금지 구조
- 확정값·임시값·미확정 사항

### 콘텐츠 카탈로그

경로: `docs/catalog/<CATALOG>.md`

- 실제 콘텐츠 ID·이름·채택 수치
- 행동·자산 요구사항
- 콘텐츠 상태

### 계산·검증

경로: `docs/balance/<BENCHMARK>.md`

- 수식·가정·입력·결과
- 수용 조건·민감도
- 재현 스크립트

### 시스템 명세

경로: `docs/spec/<SYSTEM>.md`

- 입력·출력
- 정확한 동작 순서
- 수식·동률·취소·경계 조건
- 저장·복구 상태
- 테스트 가능한 수용 조건

### 기술 설계

경로: `docs/technical/<SYSTEM>.md`

- 서버·클라이언트 책임
- 상태 소유권과 데이터 흐름
- 모듈 경계
- 저장·복구·네트워크·보안·성능

### 구현 명세

경로: `docs/implementation/<SYSTEM>.md`

- 실제 파일 경로
- 공개 타입·함수·Remote
- 설정·저장 스키마
- 오류 처리와 테스트 대상

---

## 6. 상하위 동기화

```text
README / INDEX / PROJECT_STATUS / design
        ↓
catalog 또는 balance
        ↓
spec
        ↓
technical
        ↓
implementation
        ↓
code
```

변경 영향은 반대 방향으로도 전파합니다.

```text
코드 또는 구현 명세 변경
→ 기술 설계 영향 확인
→ 시스템 명세 영향 확인
→ 카탈로그·계산 영향 확인
→ 게임 기획 영향 확인
→ 전체 루프가 바뀌면 README·INDEX 갱신
→ 완료 상태가 바뀌면 PROJECT_STATUS 갱신
```

금지되는 상태:

- 코드만 최신이고 기획이 이전 규칙인 상태
- 카탈로그와 구현이 다른 수치를 독립 보유
- 계산값을 채택 절차 없이 구현
- 계산 문서가 임시 이름을 최종 콘텐츠로 확정
- 초기화·저장·확률·수치 규칙이 문서마다 다름
- 새 시스템을 추가했지만 INDEX와 PROJECT_STATUS에 없음

---

## 7. 기능 작업 순서

### 새로운 계산

1. `PROJECT_STATUS.md`의 현재 작업인지 확인합니다.
2. 관련 design·catalog·balance 문서를 읽습니다.
3. 입력 가정과 수용 조건을 먼저 작성합니다.
4. 재현 스크립트를 작성합니다.
5. 결과와 민감도를 기록합니다.
6. 기존 계산에 미치는 영향을 재실행합니다.
7. 채택할 값은 catalog 작업으로 분리합니다.
8. `balance/INDEX.md`, `INDEX.md`, `PROJECT_STATUS.md`를 갱신합니다.

### 새로운 콘텐츠

1. 관련 계산이 잠겼는지 확인합니다.
2. 영구 ID와 상태를 정합니다.
3. 이름·행동·자산 요구사항을 작성합니다.
4. `BalanceSource`를 연결합니다.
5. 카탈로그 검증을 수행합니다.
6. `catalog/INDEX.md`, `INDEX.md`, `PROJECT_STATUS.md`를 갱신합니다.

### 새로운 기능 구현

1. 시스템 경계를 정합니다.
2. 관련 기획·카탈로그·계산을 읽습니다.
3. 시스템 명세를 작성합니다.
4. 기술 설계를 작성합니다.
5. 구현 명세를 작성합니다.
6. 코드를 구현합니다.
7. 테스트합니다.
8. 하위 변경을 상위 문서에 역방향 반영합니다.
9. README·INDEX·PROJECT_STATUS를 갱신합니다.

---

## 8. 상태와 작성 규칙

권장 메타데이터:

```markdown
# 문서 이름

- 계층: 게임 기획 | 콘텐츠 카탈로그 | 계산·검증 | 시스템 명세 | 기술 설계 | 구현 명세
- 상태: Draft | Proposed | Confirmed | Confirmed (Living Document) | Implementing | Implemented | Deprecated
- 상위 문서: `경로`
- 관련 문서: `경로`
- BalanceSource: `경로` 또는 없음
- 마지막 정리: YYYY-MM-DD
```

작성 원칙:

- 최종 결정과 짧은 이유를 남깁니다.
- 미확정 사항은 마지막에 모읍니다.
- 테스트용 계수는 임시 밸런스 값으로 표시합니다.
- 예시를 필수 규칙으로 오해하지 않게 합니다.
- 같은 설명을 불필요하게 복사하지 않고 권위 문서를 연결합니다.
- 새 문서를 만들면 해당 하위 인덱스와 `docs/INDEX.md`에 등록합니다.
- 완료 상태가 바뀌면 `PROJECT_STATUS.md`를 갱신합니다.
- 전체 루프가 바뀌면 README를 수정합니다.

---

## 9. 구현 단계 필수 검토

### 확률과 큰 수

- `BaseOddsN`을 10진수 문자열로 보존하는가
- 공식 확률 합을 임의 정밀도 유리수로 검증하는가
- 한 굴림에 정확히 한 결과인가
- 현재 행운 적용 확률을 저장·표시하지 않는가
- 타워 희귀도와 PowerBudget 순서가 일치하는가
- 코인·HP·XP가 공통 큰 수 자료형을 사용하는가

### 웨이브와 경제

- `EntryTargetSeconds`와 `FarmTargetSeconds`를 구분하는가
- 목표 시간이 남았다는 이유로 웨이브를 대기시키지 않는가
- 플레이어 DPS 기반 자동 적응 난이도가 없는가
- 보스 시간 비중과 시간당 보상이 기준 범위에 있는가
- 계획값과 직접 검증값을 구분하는가
- 카탈로그에 채택되지 않은 계산값을 구현하지 않는가

### 상태와 소유권

- 원본 상태가 서버에 있는가
- 개인 전투가 플레이어별로 격리되는가
- 프로필 원본과 파생 캐시를 구분하는가
- 영구 상태와 세션 상태를 구분하는가
- 최초 튜토리얼 플래그가 계정 영구 상태인가
- 빈 슬롯 채움이 기존 타워를 강제로 교체하지 않는가

### 저장과 복구

- 단일 프로필과 세션 잠금을 사용하는가
- 재접속·서버 이동·환생·과부하의 초기화 표가 계약과 일치하는가
- 구매·합체·환생·오프라인 지급·최초 보상이 중복 실행되지 않는가
- SchemaVersion과 순차 마이그레이션이 있는가
- 저장 실패 시 자산이 중복 지급·차감되지 않는가

### 네트워크·성능·실패

- 클라이언트는 요청만 보내고 서버가 검증하는가
- 요청 빈도와 잘못된 인자를 제한하는가
- 매 프레임 계산을 최소화했는가
- 확률 컴파일을 매 굴림마다 다시 하지 않는가
- 대상 제거·스테이지 이동·서버 종료를 처리하는가
- 누락된 카탈로그나 잘못된 맵 설정을 조용히 허용하지 않는가
- 무한 회복·분열·웨이브 정체를 데이터 검증에서 차단하는가

### UI와 피드백

- 입력 즉시 눌림·반동·음향이 있는가
- 성공과 실패가 구분되는가
- 공격 준비·판정·명중이 읽히는가
- 강한 결과가 더 강하게 느껴지는가
- 일반 화면 흔들림 없이 타격감이 전달되는가
- 파티클·카메라·UI 움직임·접근성 설정을 존중하는가

---

## 10. 충돌·미확정·승인

금지:

- 미확정 계수를 출시 확정값처럼 구현
- 사용자 승인 없이 재화·손실·제약·과금 추가
- 둘 이상의 설계안 중 하나를 조용히 확정
- 하위 문서나 코드가 상위 기획을 몰래 변경
- 공식 확률을 근사 확률로 교체
- CurrentCycle과 영구 상태를 임의 초기화
- 목표 처리시간을 강제 지속시간으로 변환
- 경제 프록시를 실제 콘텐츠 검증값으로 오인
- 계산 결과를 카탈로그 채택 없이 구현

작업을 멈추고 질문해야 하는 상황:

1. 문서끼리 충돌
2. 문서와 코드가 다름
3. 구현에 새로운 플레이어 규칙이 필요
4. 선택지에 따라 사용자 경험이 달라짐
5. 확정안을 폐기하거나 범위를 넓혀야 함

---

## 11. 구현 완료 기준

- 기획·카탈로그·계산·명세·기술 계약과 일치
- 수용 조건 충족
- 서버·클라이언트 책임 명확
- 확률·재화·타워·전투 결과 서버 검증
- 임의 정밀도 확률과 공통 큰 수 계약 준수
- 웨이브 목표시간과 경제 기준 준수
- 저장·재접속·세션 잠금·중복 요청 정의
- 최초 튜토리얼 재지급 방지
- 실패 시 자산 안전
- 성능상 명백한 낭비 없음
- UI가 성공·실패 이유 전달
- 카메라·파티클이 설정 기준 충족
- 공개 코드 계약과 구현 명세 일치
- 테스트 또는 재현 절차 존재
- README·INDEX·PROJECT_STATUS 최신

코드가 실행된다는 이유만으로 완료 처리하지 않습니다.

---

## 12. 권장 디렉터리

```text
README.md
AGENTS.md

docs/
├─ INDEX.md
├─ PROJECT_STATUS.md
├─ design/
├─ catalog/
├─ balance/
├─ spec/
├─ technical/
├─ implementation/
├─ decisions/
└─ reference/       # 이전 중인 호환 영역, 신규 작성 금지

tools/
└─ balance/
```

---

## 13. 작업 시작 전 체크리스트

```text
[ ] AGENTS.md를 읽었는가?
[ ] PROJECT_STATUS의 현재 작업을 확인했는가?
[ ] 관련 design·catalog·balance 문서를 확인했는가?
[ ] 확정안과 임시 계수를 구분했는가?
[ ] 문서·코드 충돌이 없는가?
[ ] 사용자 승인이 필요한 선택이 없는가?
[ ] 확률·큰 수·웨이브·경제·저장 영향을 확인했는가?
[ ] 변경 후 갱신할 인덱스와 체크리스트를 식별했는가?
```

## 14. 작업 종료 전 체크리스트

```text
[ ] 승인 없이 기능을 추가하지 않았는가?
[ ] 발견한 충돌을 숨기지 않았는가?
[ ] 계산과 카탈로그 책임을 분리했는가?
[ ] 공식 확률과 현재 확률 비공개 규칙을 지켰는가?
[ ] 큰 수 형식이 통일되어 있는가?
[ ] 웨이브 목표를 강제 대기시간으로 만들지 않았는가?
[ ] 저장·초기화·재접속 계약이 일치하는가?
[ ] 중복 요청과 부분 실패가 안전한가?
[ ] 코드와 구현 명세가 일치하는가?
[ ] 관련 상위 문서가 최신인가?
[ ] INDEX와 하위 인덱스를 갱신했는가?
[ ] PROJECT_STATUS의 체크박스와 다음 작업을 갱신했는가?
[ ] 미구현 항목을 완료로 표시하지 않았는가?
[ ] 테스트 또는 검증 절차를 남겼는가?
```
