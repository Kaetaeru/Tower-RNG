# Tower RNG 필수 문서·구현 가이드

이 문서는 Tower RNG의 기획, 문서 작성, 기술 설계와 실제 구현에 참여하는 사람 및 AI가 가장 먼저 읽어야 하는 필수 가이드입니다.

모든 작업은 현재 대화나 한 문서만 보고 진행하지 않습니다. 관련된 상위·하위 문서를 확인하고, 결정된 내용과 미확정 내용을 구분한 뒤 작업합니다.

---

## 1. 최우선 원칙

1. 기능을 만들거나 수정하기 전에 이 문서를 먼저 읽습니다.
2. 사용자가 확정하지 않은 기능, 수치, 예외 규칙을 임의로 추가하지 않습니다.
3. 확정안, 임시 밸런스 값, 제안, 미확정 사항을 명확히 구분합니다.
4. 하나의 문서에 서로 다른 구현 시스템을 불필요하게 합치지 않습니다.
5. 기획 의도를 코드에 바로 옮기지 않고 문서 계층을 순서대로 거칩니다.
6. 문서끼리 또는 문서와 코드가 충돌하면 임의로 한쪽을 선택하지 않습니다.
7. 둘 이상의 합리적인 선택지가 사용자 경험에 다른 영향을 주면 반드시 사용자 승인을 받습니다.
8. 구현 편의를 이유로 게임의 핵심 철학을 약화하지 않습니다.
9. 실패, 손실, 강제 접속, 빌드 함정과 불필요한 스트레스를 새로 만들지 않습니다.
10. 좋은 새 타워를 얻었을 때 기존 투자 때문에 사용하기 어려워지는 구조를 피합니다.
11. 구현 후에는 실제 코드 계약과 문서가 일치하는지 확인합니다.
12. **하위 계층의 사항이 변경되면 영향을 받는 모든 상위 계층 문서를 같은 작업에서 반드시 갱신합니다.**
13. **모든 의미 있는 UI 입력과 전투 결과는 즉각적이고 분명하며 과장된 시각·움직임·음향 피드백을 가져야 합니다.** 정적인 버튼, 움직임 없는 공격과 숫자만 변하는 결과를 완성으로 보지 않습니다.

### 상위 문서 동기화 규칙

문서 계층은 아래 방향으로 구체화되지만, 변경 영향은 반대 방향으로도 전파됩니다.

```text
README / INDEX / 게임 기획
        ↓
시스템 명세
        ↓
기술 설계
        ↓
구현 명세
        ↓
실제 코드
```

예를 들어 구현 과정에서 공개 행동이나 저장 규칙이 바뀌었다면 구현 문서만 수정해서는 안 됩니다.

```text
코드 또는 구현 명세 변경
→ 기술 설계 영향 확인 및 수정
→ 시스템 명세 영향 확인 및 수정
→ 게임 기획 영향 확인 및 수정
→ 프로젝트 전체 루프가 바뀌면 README와 INDEX 수정
```

금지되는 상태:

- 코드가 기획과 다른데 코드만 최신인 상태
- 기술 문서가 새로운 공개 규칙을 만들었지만 기획 문서에는 없는 상태
- 하위 문서에서 용어·상태·초기화 범위를 바꿨지만 상위 문서에 이전 규칙이 남은 상태
- 새 시스템을 추가했지만 README와 `docs/INDEX.md`에 연결하지 않은 상태

하위 변경이 상위 의미에 영향을 주지 않는 순수 내부 최적화라면 상위 문서를 수정할 필요는 없지만, 영향이 없는지 반드시 확인합니다.

---

## 2. 문서 계층

### 2.1 게임 기획

경로:

```text
docs/design/<SYSTEM>.md
```

다루는 내용:

- 플레이어가 경험할 기능
- 기능의 목적
- 게임 규칙의 큰 방향
- 다른 시스템과의 관계
- 허용하거나 사용하지 않는 구조
- 확정된 기본값과 임시 밸런스 값
- 아직 결정되지 않은 문제

구체적인 ModuleScript, Remote, DataStore 키와 함수 계약은 작성하지 않습니다.

### 2.2 시스템 명세

경로:

```text
docs/spec/<SYSTEM>.md
```

다루는 내용:

- 입력과 출력
- 정확한 동작 순서
- 판정 우선순위와 수식
- 동률·실패·취소·경계 조건
- 저장 또는 복구할 상태
- 테스트 가능한 수용 조건

### 2.3 기술 설계

경로:

```text
docs/technical/<SYSTEM>.md
```

다루는 내용:

- 서버와 클라이언트 책임
- 상태 소유권과 데이터 흐름
- 런타임 모듈 경계
- 저장, 복구, 네트워크와 성능
- 보안과 장애 시 안전한 동작

### 2.4 구현 명세

경로:

```text
docs/implementation/<SYSTEM>.md
```

다루는 내용:

- 실제 파일 경로
- 공개 타입과 함수
- Remote 계약
- 설정 데이터와 저장 스키마
- 오류 처리와 테스트 대상

---

## 3. 문서 분리 기준

문서는 대화 주제가 아니라 구현 책임 단위로 나눕니다.

다음 중 하나라도 해당하면 별도 문서를 우선합니다.

- 별도의 런타임 상태가 있음
- 별도의 저장 데이터가 필요함
- 독립된 UI 또는 사용자 흐름이 있음
- 별도 서비스나 모듈로 구현될 가능성이 큼
- 향후 독립적으로 변경될 가능성이 큼

반대로 작은 규칙을 의미 없이 여러 문서로 쪼개지 않습니다.

---

## 4. 기능 작업 순서

### 새 기능

1. 시스템 경계를 정합니다.
2. 관련 기존 문서와 코드를 읽습니다.
3. 게임 기획을 작성하고 사용자 승인을 받습니다.
4. 시스템 명세를 작성합니다.
5. 기술 설계를 작성합니다.
6. 구현 명세를 작성합니다.
7. 코드를 구현합니다.
8. 테스트합니다.
9. 하위 변경이 상위 문서에 모두 반영되었는지 역방향으로 검토합니다.
10. README와 INDEX의 링크·상태를 갱신합니다.

### 기존 기능 수정

1. 변경이 시작된 계층을 확인합니다.
2. 그 계층보다 아래의 계약과 코드를 수정합니다.
3. 그 계층보다 위의 문서도 영향 여부를 확인합니다.
4. 공개 행동이 바뀌었다면 반드시 상위 문서를 함께 수정합니다.
5. 한 파일만 고치고 관련 계약 문서를 방치하지 않습니다.

---

## 5. 확정 상태와 작성 규칙

문서 상단에는 가능한 한 다음 메타데이터를 사용합니다.

```markdown
# 시스템 이름

- 계층: 게임 기획 | 시스템 명세 | 기술 설계 | 구현 명세
- 상태: Draft | Confirmed | Confirmed (Living Document) | Implementing | Implemented | Deprecated
- 상위 문서: `경로`
- 하위 문서: `경로`
- 관련 문서: `경로`
- 마지막 정리: YYYY-MM-DD
```

작성 원칙:

- 최종 결정과 짧은 이유만 본문에 남깁니다.
- 미확정 사항은 마지막에 모읍니다.
- 테스트용 수치는 임시값으로 표시합니다.
- 예시를 필수 규칙으로 해석하지 않습니다.
- 같은 설명을 여러 문서에 복사하지 않고 링크로 연결합니다.
- 새로운 시스템 문서를 만들면 `docs/INDEX.md`에 즉시 등록합니다.
- 게임 전체 루프가 변하면 README도 즉시 수정합니다.

---

## 6. 구현 단계까지 반드시 고려할 항목

### 상태와 소유권

- 어떤 상태가 존재하는가
- 서버와 클라이언트 중 누가 원본을 가지는가
- 플레이어별인지 서버 공용인지
- 저장되는지 세션 전용인지

게임 결과, 재화, 타워 획득과 전투 판정은 기본적으로 서버가 검증합니다.

### 저장과 복구

- 서버 종료와 재접속 처리
- 중복 지급과 중복 차감 방지
- 스키마 변경과 마이그레이션
- 오프라인 중 진행 또는 정지 여부

### 네트워크와 보안

- 클라이언트 요청과 서버 검증
- 요청 빈도와 잘못된 인자 제한
- 개인 전투 데이터 격리
- 소유하지 않은 타워와 기능 요청 차단

### 성능

- 매 프레임 계산이 필요한지
- 주기 갱신과 캐시가 가능한지
- 타워·몬스터·투사체가 늘어날 때 비용
- 보이지 않는 다른 플레이어 전투를 불필요하게 생성하는지

### 실패와 예외

- 대상 제거, 서버 이동, 연결 종료
- 누락된 데이터와 잘못된 맵 설정
- 부분 성공으로 자산이 손실되지 않는지

### UI와 피드백

- 현재 상태를 이해할 수 있는지
- 행동할 수 없는 이유를 표시하는지
- 입력 순간 눌림·반동·음향 등 즉각적인 반응이 있는지
- 성공과 실패가 서로 다른 감각으로 구분되는지
- 공격자와 피격 대상이 판정 순간에 움직임과 효과로 반응하는지
- 중요한 결과가 일반 결과보다 강하게 느껴지는지
- 과장된 연출이 실제 판정과 다음 입력을 불필요하게 막지 않는지
- 화면 흔들림·섬광·햅틱과 감소된 움직임 설정을 지원하는지

세부 기준은 `docs/design/FEEL_AND_FEEDBACK.md`를 따릅니다.

---

## 7. 충돌·미확정·승인 처리

다음 행동을 금지합니다.

- 미확정 수치를 확정값처럼 구현
- 사용자 승인 없이 기능·재화·제약·손실 추가
- 기존 시스템과 충돌하는 규칙을 조용히 선택
- 둘 이상의 설계안 중 하나를 임의로 확정
- 하위 문서나 코드에서 상위 기획을 몰래 변경

다음 상황에서는 작업을 멈추고 사용자에게 질문합니다.

1. 문서끼리 내용이 충돌함
2. 문서와 코드가 다름
3. 구현에 새로운 게임 규칙이나 수치가 필요함
4. 선택지에 따라 사용자 경험이 달라짐
5. 확정안을 폐기하거나 범위를 넓혀야 함

질문에는 충돌 내용, 기존 결정, 선택지의 영향과 추천 이유를 포함합니다.

---

## 8. 구현 완료 기준

기능은 코드가 실행된다는 이유만으로 완료되지 않습니다.

완료 조건:

- 기획 의도와 일치
- 명세의 수용 조건 충족
- 서버와 클라이언트 책임 명확
- 재화와 주요 상태 서버 검증
- 저장·재접속·중복 요청 정의
- 실패 시 자산 안전
- 성능상 명백한 낭비 없음
- UI가 성공과 실패 이유 전달
- UI 입력과 전투 결과가 `FEEL_AND_FEEDBACK.md`의 감각 기준 충족
- 공개 코드 계약과 구현 명세 일치
- 테스트 또는 재현 절차 존재
- 하위 변경이 모든 관련 상위 문서에 반영됨
- README와 INDEX가 현재 구조를 반영함

---

## 9. 권장 디렉터리 구조

```text
README.md
AGENTS.md

docs/
├─ INDEX.md
├─ design/
├─ spec/
├─ technical/
├─ implementation/
├─ decisions/
└─ reference/
```

---

## 10. 작업 시작 전 체크리스트

```text
[ ] AGENTS.md를 읽었는가?
[ ] 관련 상위·하위 문서를 모두 확인했는가?
[ ] 확정안과 미확정 사항을 구분했는가?
[ ] 문서 또는 코드 사이에 충돌이 없는가?
[ ] 사용자 승인이 필요한 선택이 없는가?
[ ] 저장·네트워크·성능·실패 영향을 고려했는가?
[ ] 변경 후 갱신해야 할 상위 문서를 식별했는가?
[ ] UI·전투 감각 기준이 필요한 작업인지 확인했는가?
```

## 11. 작업 종료 전 체크리스트

```text
[ ] 사용자 승인 없이 기능을 추가하지 않았는가?
[ ] 발견한 충돌을 숨기지 않았는가?
[ ] 코드와 구현 명세가 일치하는가?
[ ] 기술 설계와 시스템 명세가 현재 동작을 반영하는가?
[ ] 게임 기획이 하위 변경을 반영하는가?
[ ] 프로젝트 전체 루프가 바뀌었다면 README를 수정했는가?
[ ] 새 문서와 상태가 docs/INDEX.md에 등록되었는가?
[ ] 미구현 항목을 완료로 표시하지 않았는가?
[ ] 입력·공격·피격·보상의 타격감과 반응을 검증했는가?
[ ] 테스트 또는 검증 절차를 남겼는가?
```
