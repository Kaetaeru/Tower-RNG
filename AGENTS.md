# Tower RNG 필수 문서·구현 가이드

이 문서는 Tower RNG의 기획, 명세, 기술 설계와 구현에 참여하는 사람 및 AI가 가장 먼저 읽어야 하는 필수 가이드입니다.

현재 대화나 단일 파일만 보고 작업하지 않습니다. 관련 상위·하위 문서를 확인하고 확정안·임시 밸런스 값·제안·미확정 사항을 구분합니다.

---

## 1. 최우선 원칙

1. 기능을 만들거나 수정하기 전에 이 문서를 먼저 읽습니다.
2. 사용자가 확정하지 않은 기능·수치·제약·손실을 임의로 추가하지 않습니다.
3. 확정안, 임시 밸런스 값, 제안과 미확정 사항을 명확히 구분합니다.
4. 문서끼리 또는 문서와 코드가 충돌하면 임의로 한쪽을 선택하지 않습니다.
5. 둘 이상의 합리적 선택지가 사용자 경험을 다르게 만들면 반드시 사용자 승인을 받습니다.
6. 구현 편의를 이유로 핵심 철학을 약화하지 않습니다.
7. 실패·강제 접속·빌드 함정·영구 손실과 불필요한 스트레스를 새로 만들지 않습니다.
8. 좋은 새 타워를 얻었을 때 기존 투자 때문에 사용하기 어려운 구조를 피합니다.
9. 기획 의도를 코드에 바로 옮기지 않고 문서 계층을 순서대로 거칩니다.
10. 구현 후 실제 코드 계약과 문서가 일치하는지 검증합니다.
11. **하위 계층이 변경되면 영향을 받는 모든 상위 문서를 같은 작업에서 갱신합니다.**
12. **모든 의미 있는 UI 입력과 전투 결과는 즉각적이고 분명하며 과장된 움직임·시각·음향 피드백을 가져야 합니다.**
13. **일반 UI 클릭과 일반 공격에는 화면 흔들림을 사용하지 않습니다.** 제한된 보스·환생·극희귀 사건만 `SETTINGS.md` 범위에서 허용합니다.
14. **확률·행운 구현은 `docs/design/RNG_PROBABILITY.md`를 권위 문서로 사용합니다.** 임의 정밀도 공식 분모를 일반 부동소수점 값으로 축소하거나 현재 행운 적용 확률을 UI에 노출하지 않습니다.
15. **타워·몬스터·스테이지·보상 수치는 `docs/design/BALANCE_MODEL.md`를 권위 문서로 사용합니다.** 더 희귀한 타워의 기준 기여도가 더 낮아지거나 각 시스템이 별도의 큰 수 형식을 만들 수 없습니다.
16. **저장·초기화·재접속·중복 요청 처리는 `docs/technical/STATE_LIFECYCLE.md`를 권위 계약으로 사용합니다.** 시스템마다 호환되지 않는 별도 저장 규칙을 만들지 않습니다.
17. 자동 합체와 플레이어 간 게임 자산 거래는 현재 확정 범위에 없습니다. 하위 구현에서 편의 기능으로 임의 추가하지 않습니다.
18. **웨이브 시간은 `docs/design/WAVE_PACING.md`를 권위 문서로 사용합니다.** 목표 시간을 최소 지속시간이나 강제 대기시간으로 구현하거나 플레이어 DPS에 맞춰 적을 자동 강화하지 않습니다.
19. **첫 환생 가격·문·Defense XP는 `docs/reference/FIRST_REBIRTH_ECONOMY_BENCHMARK.md`를 기준으로 사용합니다.** 잠정 수치를 다른 문서나 코드에서 독립적으로 다시 정의하지 않습니다.
20. **최초 튜토리얼은 계정당 한 번이며 정상 적·정상 보상으로 진행합니다.** 재접속·환생·중복 요청으로 첫 타워·코인·Defense XP가 재지급되지 않아야 합니다.

---

## 2. 권위 문서 지도

| 영역 | 권위 문서 | 하위 구현이 바꿀 수 없는 핵심 |
|---|---|---|
| 프로젝트 전체 | `README.md`, `docs/INDEX.md` | 최신 루프와 문서 위치 |
| 확률·행운 | `docs/design/RNG_PROBABILITY.md` | 공식 `1/N`, 임의 정밀도, 단일 결과, 현재 확률 비공개 |
| 통합 수치 | `docs/design/BALANCE_MODEL.md` | PowerBudget, StageScale, SpawnCost, 보상 인플레이션 |
| 웨이브 템포 | `docs/design/WAVE_PACING.md` | Entry·Farm 목표, 즉시 종료, 후반 상한, 자동 적응 금지 |
| 첫 환생 경제 | `docs/design/ECONOMY_PACING.md`, `docs/reference/FIRST_REBIRTH_ECONOMY_BENCHMARK.md` | 초기 가격, 문, StageCoinUnit, 첫 환생 요구량 |
| 저장·복구 | `docs/technical/STATE_LIFECYCLE.md` | 서버 권위, 단일 프로필, 튜토리얼 플래그, 세션 잠금, 원자성 |
| UI·전투 감각 | `docs/design/PRESENTATION_FEEL.md` | 과장된 반응과 완료 기준 |
| 설정·접근성 | `docs/design/SETTINGS.md` | 카메라·파티클·음향·성능 설정 |
| 개별 콘텐츠 | `docs/reference/*.md` | 실제 타워·몬스터·스테이지·노드 데이터 |

권위 문서와 충돌하는 하위 명세·코드가 발견되면 하위 구현을 조용히 기준으로 삼지 않습니다. 충돌의 원인과 영향 범위를 확인하고 사용자 승인이 필요한지 판단합니다.

---

## 3. 상하위 동기화

```text
README / INDEX / 게임 기획
        ↓
시스템 명세
        ↓
기술 설계
        ↓
구현 명세
        ↓
실제 코드
```

변경 영향은 반대 방향으로도 전파됩니다.

```text
코드 또는 구현 명세 변경
→ 기술 설계 영향 확인
→ 시스템 명세 영향 확인
→ 게임 기획 영향 확인
→ 전체 루프가 바뀌면 README·INDEX 갱신
```

금지되는 상태:

- 코드만 최신이고 기획이 이전 규칙인 상태
- 기술 문서가 새로운 공개 규칙을 만들었지만 기획에는 없는 상태
- 초기화·저장·확률·수치 규칙이 문서마다 다른 상태
- 새 시스템을 추가했지만 README와 INDEX에 연결하지 않은 상태

순수 내부 최적화라면 상위 문서를 수정하지 않아도 되지만 공개 행동에 영향이 없는지 반드시 확인합니다.

---

## 4. 문서 계층

### 게임 기획

경로: `docs/design/<SYSTEM>.md`

- 플레이어 경험과 목적
- 큰 규칙과 시스템 관계
- 허용·금지 구조
- 확정값·임시값·미확정 사항

### 시스템 명세

경로: `docs/spec/<SYSTEM>.md`

- 입력·출력
- 정확한 동작 순서
- 수식·동률·취소·경계 조건
- 저장·복구 상태
- 테스트 가능한 수용 조건

### 기술 설계

경로: `docs/technical/<SYSTEM>.md`

- 서버·클라이언트 책임
- 상태 소유권과 데이터 흐름
- 모듈 경계
- 저장·복구·네트워크·보안·성능

### 구현 명세

경로: `docs/implementation/<SYSTEM>.md`

- 실제 파일 경로
- 공개 타입·함수·Remote
- 설정·저장 스키마
- 오류 처리와 테스트 대상

### 참조 데이터

경로: `docs/reference/<CATALOG>.md`

- 실제 콘텐츠 ID와 수치
- 카탈로그 검증 항목
- Proposed·Confirmed·Implemented 상태

---

## 5. 기능 작업 순서

### 새 기능

1. 시스템 경계를 정합니다.
2. 관련 문서와 코드를 읽습니다.
3. 게임 기획을 작성하고 필요한 사용자 승인을 받습니다.
4. 시스템 명세를 작성합니다.
5. 기술 설계를 작성합니다.
6. 구현 명세를 작성합니다.
7. 코드를 구현합니다.
8. 테스트합니다.
9. 하위 변경을 상위 문서에 역방향 반영합니다.
10. README와 INDEX를 갱신합니다.

### 기존 기능 수정

1. 변경이 시작된 계층을 확인합니다.
2. 하위 계약과 코드를 수정합니다.
3. 상위 의미의 영향 여부를 확인합니다.
4. 공개 행동이 바뀌면 상위 문서를 함께 수정합니다.
5. 한 파일만 고치고 관련 계약을 방치하지 않습니다.

---

## 6. 상태와 작성 규칙

문서 상단 권장 메타데이터:

```markdown
# 시스템 이름

- 계층: 게임 기획 | 시스템 명세 | 기술 설계 | 구현 명세 | 참조 데이터
- 상태: Draft | Proposed | Confirmed | Confirmed (Living Document) | Implementing | Implemented | Deprecated
- 상위 문서: `경로`
- 하위 문서: `경로`
- 관련 문서: `경로`
- 마지막 정리: YYYY-MM-DD
```

작성 원칙:

- 최종 결정과 짧은 이유를 남깁니다.
- 미확정 사항은 마지막에 모읍니다.
- 테스트용 계수는 임시 밸런스 값으로 표시합니다.
- 예시를 필수 규칙으로 오해하지 않게 합니다.
- 같은 설명을 불필요하게 복사하지 않고 권위 문서를 연결합니다.
- 새 문서를 만들면 INDEX에 등록합니다.
- 전체 루프가 바뀌면 README를 수정합니다.

---

## 7. 구현 단계 필수 검토

### 확률과 큰 수

- `BaseOddsN`을 10진수 문자열로 보존하는가
- 공식 확률 합을 임의 정밀도 유리수로 검증하는가
- 한 굴림에 정확히 한 결과인가
- 현재 행운 적용 확률을 저장·표시하지 않는가
- 타워 희귀도와 PowerBudget 순서가 일치하는가
- 코인·HP·XP가 공통 큰 수 자료형을 사용하는가

### 웨이브와 경제

- `EntryTargetSeconds`와 `FarmTargetSeconds`를 구분하는가
- 목표 시간이 남았다는 이유로 웨이브를 대기시키지 않는가
- 플레이어 DPS 기반 자동 적응 난이도가 없는가
- 보스 시간 비중과 시간당 보상이 기준 범위에 있는가
- 초기 가격·문·첫 환생 값이 경제 벤치마크와 일치하는가
- 상위 스테이지 가정값을 실제 검증값처럼 표시하지 않는가

### 상태와 소유권

- 원본 상태가 서버에 있는가
- 개인 전투가 플레이어별로 격리되는가
- 프로필 원본과 파생 캐시를 구분하는가
- 영구 상태와 세션 상태를 구분하는가
- 최초 튜토리얼 플래그가 계정 영구 상태인가
- 빈 슬롯 채움이 기존 타워를 강제로 교체하지 않는가

### 저장과 복구

- 단일 프로필과 세션 잠금을 사용하는가
- 재접속·서버 이동·환생·과부하의 초기화 표가 계약과 일치하는가
- 구매·합체·환생·오프라인 지급·최초 보상이 중복 실행되지 않는가
- SchemaVersion과 순차 마이그레이션이 있는가
- 저장 실패 시 자산이 중복 지급·차감되지 않는가

### 네트워크와 보안

- 클라이언트는 요청만 보내고 서버가 검증하는가
- 요청 빈도와 잘못된 인자를 제한하는가
- 소유하지 않은 타워·기능 요청을 차단하는가
- TransactionId, 사건 ID 또는 MutationSequence가 필요한가

### 성능

- 매 프레임 계산이 반드시 필요한가
- 캐시·주기 갱신·공간 인덱스를 사용할 수 있는가
- 타워·몬스터·투사체 증가 비용을 측정했는가
- 보이지 않는 다른 플레이어 전투를 불필요하게 생성하지 않는가
- 큰 수와 확률 컴파일을 매 굴림마다 처음부터 계산하지 않는가

### 실패와 예외

- 대상 제거·스테이지 이동·서버 종료를 처리하는가
- 누락된 카탈로그·잘못된 맵 설정을 조용히 허용하지 않는가
- 부분 성공으로 자산이 손실되지 않는가
- 무한 회복·분열·웨이브 정체를 데이터 검증에서 차단하는가
- 최초 튜토리얼 중 종료 후 다음 미완료 단계부터 안전하게 재개하는가

### UI와 피드백

- 입력 즉시 눌림·반동·음향이 있는가
- 성공과 실패가 구분되는가
- 공격 준비·판정·명중이 읽히는가
- 강한 결과가 더 강하게 느껴지는가
- 일반 화면 흔들림 없이 타격감이 전달되는가
- 파티클·카메라·UI 움직임·접근성 설정을 존중하는가

---

## 8. 충돌·미확정·승인

금지:

- 미확정 계수를 출시 확정값처럼 구현
- 사용자 승인 없이 재화·손실·제약·과금 추가
- 둘 이상의 설계안 중 하나를 조용히 확정
- 하위 문서나 코드가 상위 기획을 몰래 변경
- 구현 편의를 위해 정확한 공식 확률을 근사 확률로 교체
- 저장 편의를 위해 CurrentCycle과 영구 상태를 임의 초기화
- 목표 처리시간을 강제 지속시간으로 변환
- 경제 프록시를 실제 콘텐츠 검증값으로 오인

작업을 멈추고 질문해야 하는 상황:

1. 문서끼리 충돌
2. 문서와 코드가 다름
3. 구현에 새로운 플레이어 규칙이 필요
4. 선택지에 따라 사용자 경험이 달라짐
5. 확정안을 폐기하거나 범위를 넓혀야 함

질문에는 기존 결정, 충돌 내용, 각 선택지의 영향과 추천 이유를 포함합니다.

---

## 9. 구현 완료 기준

- 기획·명세·기술 계약과 일치
- 수용 조건 충족
- 서버·클라이언트 책임 명확
- 확률·재화·타워·전투 결과 서버 검증
- 임의 정밀도 확률과 공통 큰 수 계약 준수
- 웨이브 목표시간과 경제 기준 준수
- 저장·재접속·세션 잠금·중복 요청 정의
- 최초 튜토리얼 재지급 방지
- 실패 시 자산 안전
- 성능상 명백한 낭비 없음
- UI가 성공·실패 이유 전달
- 입력·공격·피격이 `PRESENTATION_FEEL.md` 기준 충족
- 카메라·파티클이 `SETTINGS.md` 기준 충족
- 공개 코드 계약과 구현 명세 일치
- 테스트 또는 재현 절차 존재
- 관련 상위 문서와 README·INDEX 최신

코드가 실행된다는 이유만으로 완료 처리하지 않습니다.

---

## 10. 권장 디렉터리

```text
README.md
AGENTS.md

docs/
├─ INDEX.md
├─ design/
├─ spec/
├─ technical/
├─ implementation/
├─ decisions/
└─ reference/
```

---

## 11. 작업 시작 전 체크리스트

```text
[ ] AGENTS.md를 읽었는가?
[ ] 관련 권위 문서를 확인했는가?
[ ] 확정안과 임시 계수를 구분했는가?
[ ] 문서·코드 충돌이 없는가?
[ ] 사용자 승인이 필요한 선택이 없는가?
[ ] 확률·큰 수·웨이브·경제·저장 생명주기 영향을 확인했는가?
[ ] 네트워크·성능·실패 영향을 고려했는가?
[ ] 변경 후 갱신할 상위 문서를 식별했는가?
[ ] UI·전투 감각과 설정 기준을 확인했는가?
```

## 12. 작업 종료 전 체크리스트

```text
[ ] 승인 없이 기능을 추가하지 않았는가?
[ ] 발견한 충돌을 숨기지 않았는가?
[ ] 공식 확률과 현재 확률 비공개 규칙을 지켰는가?
[ ] 큰 수 형식이 통일되어 있는가?
[ ] 웨이브 목표를 강제 대기시간으로 만들지 않았는가?
[ ] 첫 경제 값이 벤치마크와 일치하는가?
[ ] 저장·초기화·재접속 계약이 일치하는가?
[ ] 최초 튜토리얼 보상이 중복 지급되지 않는가?
[ ] 중복 요청과 부분 실패가 안전한가?
[ ] 코드와 구현 명세가 일치하는가?
[ ] 기술 설계·시스템 명세·게임 기획이 최신인가?
[ ] README와 INDEX를 갱신했는가?
[ ] 미구현 항목을 완료로 표시하지 않았는가?
[ ] 일반 화면 흔들림 없이 타격감을 검증했는가?
[ ] 테스트 또는 검증 절차를 남겼는가?
